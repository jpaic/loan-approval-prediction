import raw from "@/public/dashboard-data.json";
import type { DashboardData } from "./types";

// dashboard-data.json is generated by scripts/export_dashboard_data.py and
// copied into public/ at that path. It is imported statically here so the
// site ships as pre-rendered HTML/JSON with no Python/ML runtime needed.
export const dashboardData = raw as unknown as DashboardData;
